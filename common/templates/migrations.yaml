{{- if .Values.global.prehooks.dbMigrations.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "charts-common.name" . }}-migrations
  namespace: "{{ .Release.Namespace }}"
  labels:
    {{- include "charts-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-1"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      {{- with .Values.global.deployment.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "charts-common.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.global.deployment.image.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "charts-common.serviceAccountName" . }}
      restartPolicy: Never
      {{- if or .Values.global.configMap.enabled (and (eq .Values.global.destination "gcp") .Values.global.secrets.enabled) }}
      volumes:
        {{- if .Values.global.configMap.enabled }}
        - name: app-config
          configMap:
            name: {{ .Values.global.configMap.name }}
            items:
              - key: {{ .Values.global.configMap.fileName }}
                path: {{ .Values.global.configMap.fileName }}
            defaultMode: 420
        {{- end }}
        {{- if and (eq .Values.global.destination "gcp") .Values.global.secrets.enabled }}
        - name: gcpsecrets
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "charts-common.name" . }}
        {{- end }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.global.deployment.image.uri }}"
          imagePullPolicy: {{ .Values.global.deployment.image.pullPolicy }}
          {{- if .Values.global.prehooks.dbMigrations.command }}
          command: {{- toYaml .Values.global.prehooks.dbMigrations.command | nindent 12 }}
          {{- end }}
          {{- if .Values.global.prehooks.dbMigrations.args }}
          args: {{- toYaml .Values.global.prehooks.dbMigrations.args | nindent 12 }}
          {{- end }}
          {{- if or .Values.global.configMap.enabled (and (eq .Values.global.destination "gcp") .Values.global.secrets.enabled) }}
          volumeMounts:
            {{- if .Values.global.configMap.enabled }}
            - name: app-config
              readOnly: true
              mountPath: {{ .Values.global.configMap.mountPath }}
              subPath: {{ .Values.global.configMap.fileName }}
            {{- end }}
            {{- if and (eq .Values.global.destination "gcp") .Values.global.secrets.enabled }}
            {{- range .Values.global.secrets.data }}
            - name: gcpsecrets
              mountPath: {{ $.Values.global.secrets.mountPath }}/{{ .fileName }}
              subPath: {{ .fileName }}
              readOnly: true
            {{- end }}
            {{- end }}
          {{- end }}
          env:
            {{- toYaml .Values.global.deployment.env | nindent 12 }}
          resources:
            {{- toYaml .Values.global.deployment.resources | nindent 12 }}
{{- end }}